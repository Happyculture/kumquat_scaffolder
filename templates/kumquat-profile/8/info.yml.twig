name: {{ profile }}
type: profile
description: '{{ profile }} install profile'
core_version_requirement: ^8 || ^9

{% if themes %}

themes:
{% for theme in themes %}
  - '{{ theme }}'
{% endfor %}
{% endif %}
