name: {{ profile }}
type: profile
description: '{{ profile }} install profile'
core_version_requirement: ^8 || ^9 || ^10
version: 1.0.0
{% if themes %}

themes:
{% for theme in themes %}
  - '{{ theme }}'
{% endfor %}
{% endif %}
