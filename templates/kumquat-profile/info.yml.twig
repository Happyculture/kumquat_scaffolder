name: {{ name }}
type: profile
description: '{{ name }} install profile'
core_version_requirement: ^10 || ^11
version: 1.0.0
{% if themes %}

themes:
{% for theme in themes %}
  - '{{ theme }}'
{% endfor %}
{% endif %}
