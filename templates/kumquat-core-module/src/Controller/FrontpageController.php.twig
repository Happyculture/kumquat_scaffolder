<?php

declare(strict_types=1);

namespace Drupal\{{ machine_name }}\Controller;

use Drupal\Core\Controller\ControllerBase;

/**
 * Returns responses for CCU Core routes.
 */
final class FrontpageController extends ControllerBase {

  /**
   * Builds the response.
   */
  public function frontpage(): array {

    $build['content'] = [
      '#type' => 'item',
      '#markup' => $this->t('Work in progress!'),
    ];

    return $build;
  }

}
