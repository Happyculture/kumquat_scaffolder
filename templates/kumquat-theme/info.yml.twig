name: {{ theme }}
type: theme
description: '{{ theme }} theme'
package: {{ profile }}
core_version_requirement: ^8 || ^9 || ^10
base theme: classy

regions:
  branding: Branding
  navigation: Navigation
  highlighted: Highlighted
  help: Help
  content: Content
  footer: Footer
  # Hidden regions.
  page_bottom: 'Page bottom'
  page_top: 'Page top'

libraries:
  - {{ machine_name }}/global
  - {{ machine_name }}/off_canvas_menu

ckeditor_stylesheets:
  - dist/css/ckeditor.css
